# Aegis web dashboard

The dashboard is a Next.js, TypeScript, and Tailwind risk-operations interface backed by the
Aegis API.

```bash
cp .env.example .env.local
npm install
npm run dev
```

Set `NEXT_PUBLIC_API_BASE_URL` when the API is not running at `http://localhost:8000`.
